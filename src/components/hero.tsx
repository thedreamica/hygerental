import { Box, Typography } from "@mui/material";

const HeroSection = () => {
  return (
    <Box>
      <Box>
        <Typography>Hello World</Typography>
      </Box>
    </Box>
  );
};

export default HeroSection;
